# nnU-Net segmentation

This directory contains the `Dataset800_PD` metadata template used by the four-fold segmentation
workflow. Clinical images, labels, case assignments, predictions, and weights must remain outside
the repository.

